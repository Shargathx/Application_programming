## Configuration

This application uses environment variables for configuration (defined in a `.env` file):

- **`PORT`**: Specifies the port the server listens on (defaults to `3000`)[cite: 9].
- **`TASKS_FILE`**: Specifies the file path where tasks are saved and loaded (defaults to `./data/tasks.json`)[cite: 9].
