/*

Component composition
Building complex UIs by combining smaller, independent components together.

The children prop
A special prop that automatically captures and renders anything nested between a component's opening and closing tags.

Reusing a layout without repeating its markup
Wrapping unique content inside a shared structural template to keep code DRY and consistent.

*/
